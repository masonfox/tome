[test]
preload = ["./test-setup.ts"]
# Run tests serially to prevent database context race conditions
# AsyncLocalStorage support exists but isn't fully integrated - stack trace parsing fails in CI
concurrency = 1
