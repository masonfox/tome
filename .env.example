# Calibre Integration
# Path to your Calibre library folder (metadata.db is expected at the root)
# For local development: /path/to/calibre/library
# For Docker: /calibre (after mounting your library)
CALIBRE_LIBRARY_PATH=/path/to/calibre/library

# MongoDB
# For local development: mongodb://localhost:27017/tome
# For Docker: mongodb://mongodb:27017/tome
MONGODB_URI=mongodb://localhost:27017/tome

# Application
PORT=3000
NODE_ENV=development

# Optional: simple password protections
AUTH=FALSE
